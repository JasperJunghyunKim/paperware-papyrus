<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="/stylesheets/style.css"/>
    <title>팝빌 SDK Express Example. </title>
</head>

<body>
<div id="content">
    <p class="heading1">Response</p>
    <br/>
    <fieldset class="fieldset1">
        <legend><%= path %></legend>
        <fieldset class="fieldset2">
            <legend> 카카오톡 전송내역 목록 조회</legend>
            <ul>
                <li>code (상태코드) : <%= result.code %></li>
                <li>message (응답 메시지) : <%= result.message %></li>
                <li>total (총 검색결과 건수) : <%= result.total %></li>
                <li>perPage (페이지당 검색개수) : <%= result.perPage %></li>
                <li>pageNum (페이지 번호) : <%= result.pageNum %></li>
                <li>pageCount (페이지 개수) : <%= result.pageCount %></li>
                <% result.list.forEach(function (list, index) { %>
                    <fieldset class="fieldset3">
                        <legend> 카카오톡 전송내역 조회 결과 [ <%= index + 1 %> / <%= result.list.length %>]</legend>
                        <ul>
                            <li>state (전송상태 코드) : <%= list.state %></li>
                            <li>sendDT (전송일시) : <%= list.sendDT %></li>
                            <li>result (전송결과 코드) : <%= list.result %></li>
                            <li>resultDT (전송결과 수신일시) : <%= list.resultDT %> </li>
                            <li>contentType (카카오톡 유형) : <%= list.contentType %></li>
                            <li>receiveNum (수신번호) : <%= list.receiveNum %></li>
                            <li>receiveName (수신자명) : <%= list.receiveName %></li>
                            <li>content (알림톡/친구톡 내용) : <%= list.content %></li>
                            <li>altSubject (대체문자 제목) : <%= list.altSubject %></li>
                            <li>altContent (대체문자 내용) : <%= list.altContent %></li>
                            <li>altContentType (대체문자 전송타입) : <%= list.altContentType %></li>
                            <li>altSendDT (대체문자 전송일시) : <%= list.altSendDT %></li>
                            <li>altResult (대체문자 전송결과 코드) : <%= list.altResult %></li>
                            <li>altResultDT (대체문자 전송결과 수신일시) : <%= list.altResultDT %></li>
                            <li>receiptNum (접수번호) : <%= list.receiptNum %></li>
                            <li>requestNum (요청번호) : <%= list.requestNum %></li>
                            <li>interOPRefKey (파트너 지정 키) : <%= list.interOPRefKey %></li>
                        </ul>
                    </fieldset>
                <% }); %>
            </ul>
        </fieldset>
    </fieldset>
</div>
</body>
