<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="/stylesheets/style.css"/>
    <title>팝빌 SDK Express Example. </title>
</head>

<body>
<div id="content">
    <p class="heading1">Response</p>
    <br/>
    <fieldset class="fieldset1">
        <legend><%= path %></legend>
        <fieldset class="fieldset2">
            <ul>
                <%
                result.forEach(function(emailConfig, index){
                %>
                    <% if(emailConfig.emailType === "SMT_ISSUE") { %>
                        <li><%= emailConfig.emailType %> (수신자에게 전자명세서가 발행 되었음을 알려주는 메일 전송 여부) : <%= emailConfig.sendYN %></li>
                    <% } %>
                    <% if(emailConfig.emailType === "SMT_ACCEPT") { %>
                        <li><%= emailConfig.emailType %> (발신자에게 전자명세서가 승인 되었음을 알려주는 메일 전송 여부) : <%= emailConfig.sendYN %></li>
                    <% } %>
                    <% if(emailConfig.emailType === "SMT_DENY") { %>
                        <li><%= emailConfig.emailType %> (발신자에게 전자명세서가 거부 되었음을 알려주는 메일 전송 여부) : <%= emailConfig.sendYN %></li>
                    <% } %>
                    <% if(emailConfig.emailType === "SMT_CANCEL") { %>
                        <li><%= emailConfig.emailType %> (수신자에게 전자명세서가 취소 되었음을 알려주는 메일 전송 여부) : <%= emailConfig.sendYN %></li>
                    <% } %>
                    <% if(emailConfig.emailType === "SMT_CANCEL_ISSUE") { %>
                        <li><%= emailConfig.emailType %> (수신자에게 전자명세서가 발행취소 되었음을 알려주는 메일 전송 여부) : <%= emailConfig.sendYN %></li>
                    <% } %>
                <%
                });
                %>
            </ul>
        </fieldset>
    </fieldset>
</div>
</body>

</html>
